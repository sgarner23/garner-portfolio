import React from "react";
import "./ProjectContainer.css";

const ProjectContainer = ({ children }) => {
  return <div className="main-project-container">{children}</div>;
};

export default ProjectContainer;
